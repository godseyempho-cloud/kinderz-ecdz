{
  "name": "kinderz-ecdz",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "postinstall": "prisma generate --schema=packages/db/prisma/schema.prisma"
  },
  "dependencies": {
    "next": "^15.5.14",
    "prisma": "5.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "turbo": "latest"
  }
} 